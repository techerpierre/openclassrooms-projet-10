export const selectAccounts = (state) => state.accounts.accounts.data;
export const selectAccountsPending = (state) => state.accounts.accounts.pending;
export const selectAccountsError = (state) => state.accounts.accounts.error;